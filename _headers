# Cloudflare Pages per-path headers.
# Every per-prospect path gets noindex (deploy.py appends a block per slug).
# The root sample stays indexable.
/anw/*
  X-Robots-Tag: noindex

/brittneybatla-7uqq/*
  X-Robots-Tag: noindex

/manifestationbabe-a9m9/*
  X-Robots-Tag: noindex
